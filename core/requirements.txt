# ───── حزم Django الأساسية ─────
Django==5.1.2
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0

# ───── قواعد بيانات وملفات ثابتة ─────
psycopg[binary]>=3.2.2,<3.3     # إصدار أحدث متوافق مع Python 3.13
whitenoise==6.7.0               # لخدمة staticfiles في الإنتاج
gunicorn==23.0.0                # سيرفر الإنتاج على Render

# ───── مكتبات المعالجة والتحليل ─────
Pillow==10.4.0
numpy==2.1.3
opencv-python-headless==4.10.0.84
requests==2.32.3

# ───── .env ─────
python-dotenv==1.0.1

# ───── أدوات تطوير (محلياً فقط، لكن وجودها في requirements لا يضر) ─────
black==24.8.0
ruff==0.6.9
pytest==8.3.2
pytest-django==4.9.0
